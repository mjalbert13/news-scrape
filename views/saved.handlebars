<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        <li class="nav-item active">
            <a class="nav-link home" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link scrape" id="scrape" href="/scrape">Scrape New Artilces</a>
        </li>
        <li class="nav-item">
            <a class="nav-link saved" id="saved" href="/saved">Saved Articles</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="clear" href="">Clear Articles</a>
        </li>
        </ul>
    </div>
</nav>

<div class="container">
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">Saved Articles!</h1>
            <p class="lead">Your own library</p>
        </div>
    </div>

    <div class="articles">
        {{#each articles}}
            {{#if saved}}
            <div class="article">
                <a href="{{link}}"><h2>{{title}}</h2></a>
                <p>{{summary}}</p>
                <button class="btn btn-primary unsave" data-id="{{_id}}">UNSAVE</button>
                <button class="btn btn-primary addNote" data-id="{{_id}}">NOTE</button>
                <br>
            </div>
            {{/if}}
        {{/each}}
    </div>
</div>
 {{!-- modal for displaying and creating notes --}}
  <div id="noteModal" class="modal fade" role="dialog">
    <div class="modal-dialog modal-md">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title text-center">Add a note</h4>
        </div>
        <div id="notes" class="modal-body"></div>
      </div>
    </div>
</div>